# 小动物餐厅 AI 协作说明

本文件适用于整个仓库。AI进入项目后先阅读本文件，并根据任务打开下方对应文档。不要只根据代码猜测产品意图。

## 项目目标

这是给2～3岁儿童和照护者一起玩的离线H5厨房游戏。孩子给小兔子、小熊准备食物，在角色扮演中接触分类、数字1～5、实际数量和逐个计数。父母可以录制完整台词，为动物角色配音。

产品用于家庭共同游戏，不是课程、测试或儿童发展评估。不要用得分、速度、错误次数或使用时长评价孩子。

## 开始任务前读取对应资料

| 任务 | 必须先读 |
| --- | --- |
| 产品范围、玩法或文案 | `docs/PROJECT_CONTEXT.md`、`docs/PRD.md` |
| 儿童界面、触控、插画或声音体验 | `docs/PRODUCT_DESIGN.md` |
| 状态、存储、音频、离线或构建 | `docs/ARCHITECTURE.md` |
| 改变既有取舍 | `docs/DECISIONS.md` |
| 双设备同步、音色生成等新方向 | `docs/ROADMAP.md` |
| 测试和发布 | `docs/ACCEPTANCE.md`、`docs/DEPLOYMENT.md` |
| 提交和协作流程 | `docs/CONTRIBUTING.md` |

完整导航见 `docs/README.md`。`docs/superpowers/plans/` 是首版历史计划，不是当前需求基线。

## 当前已经实现的范围

- 角色：小兔子、小熊。
- 食物：水饺、大虾、排骨、西蓝花、青菜、香菇、包子。
- 数量：1～5，可选1～3或指定数量。
- 模式：动物点餐、食物分类、自由厨房。
- 平台：iPad Safari和Android Chrome，可添加到桌面并在资源准备后离线运行。
- 配音：内置完整语音，家长可按角色逐句录制、试听、恢复默认、导出和导入。
- 数据：设置和会话保存在localStorage，录音保存在IndexedDB；无账号、无业务后端、无第三方分析。

双设备自动同步和用少量录音生成完整音色包仍在评估，不得描述为当前能力。当前两台设备通过导出、传递、导入语音包共享录音，游戏进度不合并。

## 不得破坏的产品规则

1. 点餐一次只有一种食物和一个数量。锅里只提供订单所需的准确份数；取走后减少，拿回后恢复，不自动补充。
2. 分类和自由厨房每轮只选三种食物，每种两个。增加食谱不能增加单屏类别或缩小触摸区。
3. 数量凑齐时不自动提交。孩子主动点“上菜”，错误后保留盘子并允许调整。
4. “数一数”是可选帮助。每个物体一次最多计数一次，按孩子点按节奏播放，盘子变化后清除旧标记。
5. 不使用扣分、排行榜、红叉、失败警报、动物生气、倒数催促或无限续局。
6. 核心触摸目标至少72×72 CSS像素；点击必须能完成操作，拖动只能作为补充。
7. 不只靠颜色区分食物、目标或状态。食物在实际尺寸下应不看文字也容易辨认。
8. 家人声音是在扮演动物角色。不要把预录声音描述为实时陪伴，也不要让录音缺失阻断游戏。
9. 孩子端不申请麦克风、不监听回答。麦克风只在家长主动录音时启用，退出、取消和切后台时释放。
10. 达到轮数或时间后温和收尾，不要求做对才能结束，不自动开始下一局。

## 食物视觉线索

- 大虾要保留弯曲身体、头、眼睛、触须、分节和尾部。
- 排骨是一块肉，白色骨头只从边缘短短露出。
- 所有食物在锅、盘、订单和分类目标中使用同一套图案。
- 幼儿试用中的辨认结果优先于成人对装饰效果的偏好。

## 技术约束

- 使用原生JavaScript ES modules、CSS、内联SVG、Web Audio、IndexedDB和Service Worker，无第三方运行时依赖。
- 游戏规则放在 `public/js/model.js` 的纯状态转换中。DOM事件只分发动作，不维护第二份游戏状态。
- 每个食物有稳定ID。取放改变位置，不创建副本；点餐库存与订单数量守恒。
- 录音导入必须先完整校验，再经家长确认，以单次IndexedDB事务写入；失败保留已有录音。
- 离线更新先下载完整资源到临时缓存，验证成功后再切换；游戏中不得自动刷新。
- 修改 `public/` 后运行构建，使资源清单和Service Worker版本同步更新。

## 隐私和公开仓库规则

- 不上传家人录音、孩子姓名或影像、设备标识、浏览器数据、Access Token或服务密钥。
- 不把声音模型、生成服务密钥或第三方凭据放进H5代码。
- 新增云同步、远程声音生成、分析或账号前，先更新PRD、数据流程、授权和删除机制。
- 这是个人项目。Git提交使用仓库本地配置的GitHub隐私邮箱，不使用公司姓名、邮箱或凭据。

## 修改和验证

遵守根环境的通用提交格式：`type(scope): 中文描述`。代码注释使用中文。

规则或数据变化至少运行：

```bash
npm test
npm run build
```

页面、触控、录音、备份或离线流程变化还要运行相应浏览器测试。自动化不能替代iPad Safari和Android Chrome上的真实触摸、麦克风、安装和断网启动检查。

改变儿童可见行为时同步更新 `docs/PRD.md` 和 `docs/PRODUCT_DESIGN.md`；改变架构或数据格式时更新 `docs/ARCHITECTURE.md`；做出新的长期取舍时更新 `docs/DECISIONS.md`。讨论中的想法先进入 `docs/ROADMAP.md`，确认后再写入PRD。
